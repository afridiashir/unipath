DATABASE_URL="postgresql://user:password@localhost:5432/unipath?schema=public"
# Direct (non-pooled) connection for `prisma migrate`. On Neon this is the same
# URL as DATABASE_URL without the `-pooler` segment in the host.
DIRECT_URL="postgresql://user:password@localhost:5432/unipath?schema=public"
